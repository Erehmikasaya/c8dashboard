{
  "name": "c8-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Cantor8 Multi-VPS Monitoring Dashboard — Vercel Serverless",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site, no build needed'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  },
  "engines": {
    "node": ">=18"
  }
}
